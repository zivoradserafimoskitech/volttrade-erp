# VoltTrade Analytics Service — Python Dependencies
# Install: pip install -r requirements.txt

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
numpy>=1.24.0
pandas>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
scipy>=1.11.0

# Forecasting
lightgbm>=4.1.0
xgboost>=2.0.0
scikit-learn>=1.3.0

# Optimization
pulp>=2.7.0

# Deep Learning (LSTM / GRU / CNN / TFT) — CPU-only wheels.
# The default PyPI torch wheel bundles CUDA (~2.5 GB) and will not fit the
# instance; the CPU index below keeps the image around 300 MB.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
# NOTE: pytorch-forecasting is intentionally NOT used — the compact TFT in
# models/deep_seq.py reproduces its gating/attention at CPU-affordable size.

# Data ingestion
# entsoe-py>=0.6.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
